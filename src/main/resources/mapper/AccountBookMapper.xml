<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.asset.mapper.AccountBookMapper">

    <select id="selectTransactionsByMonth" resultType="com.example.asset.dto.TransactionDto">
        SELECT id, type, category, amount, transaction_date AS transactionDate, memo
        FROM transactions
        WHERE DATE_FORMAT(transaction_date, '%Y-%m') = #{yearMonth}
        ORDER BY transaction_date DESC, id DESC
    </select>

    <insert id="insertTransaction" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO transactions (type, category, amount, transaction_date, memo)
        VALUES (#{type}, #{category}, #{amount}, #{transactionDate}, #{memo})
    </insert>

    <delete id="deleteTransaction">
        DELETE FROM transactions WHERE id = #{id}
    </delete>

</mapper>