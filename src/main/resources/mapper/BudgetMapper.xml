<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.asset.mapper.BudgetMapper">

    <select id="selectBudgetByMonth" resultType="com.example.asset.dto.BudgetDto">
        SELECT id, year_month AS yearMonth, amount
        FROM budgets
        WHERE year_month = #{yearMonth}
    </select>

    <insert id="upsertBudget">
        INSERT INTO budgets (year_month, amount)
        VALUES (#{yearMonth}, #{amount})
        ON DUPLICATE KEY UPDATE amount = #{amount}
    </insert>

</mapper>