server:
  port: 8080

spring:
  datasource:
    driver-class-name: org.mariadb.jdbc.Driver
    url: jdbc:mariadb://localhost:3306/board_db?useSSL=false&characterEncoding=UTF-8
    username: root
    password: totoro

mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.example.asset.dto
  configuration:
    map-underscore-to-camel-case: true